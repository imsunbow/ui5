<mvc:View controllerName="code1.cl5.crud.p03.code1cl5crudp03.controller.CRUDViewpractice03"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:t="sap.ui.table"
    xmlns:f="sap.ui.layout.form"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">

    

    <t:Table
        id="matList"
        rows="{/MatSet}"
        visibleRowCount="7"
        selectionMode="Single" 
        selectionBehavior="RowOnly"
        paste="onPaste"
        ariaLabelledBy="title">

        <t:extension>

            <!-- OverflowToolbar => 입력 필드들을 받을 공간 할당 및 ID 부여 -->
            <OverflowToolbar style="Clear">
                <!-- search -->
                <Button icon="sap-icon://search" text="Search" press="onSearch"/>
                <ToolbarSpacer width="2rem"/>
                <Input id="IMatnr" placeholder="Matnr" width="6rem" maxLength="10" showClearIcon="true"/>
                <Input id="IAuart" placeholder="Auart" width="6rem" maxLength="10" showClearIcon="true"/>                
            </OverflowToolbar>

        </t:extension>

         <t:columns>

            <t:Column width="10rem">
                <Label text="Matnr" />
                <t:template>
                    <Text text="{Matnr}" wrapping="false" />
                </t:template>
            </t:Column>

            <t:Column width="10rem">
                <Label text="Auart" />
                <t:template>
                    <Text text="{Auart}" wrapping="false" />
                </t:template>
            </t:Column>

            <t:Column width="10rem">
                <Label text="Vkorg" />
                <t:template>
                    <Text text="{Vkorg}" wrapping="false" />
                </t:template>
            </t:Column>

            <t:Column width="10rem">
                <Label text="Spart" />
                <t:template>
                    <Text text="{Spart}" wrapping="false" />
                </t:template>
            </t:Column>

        </t:columns>

    </t:Table>

    <OverflowToolbar style="Clear">
        <Label text="Material List" width="10rem"/>
        <ToolbarSpacer width="25rem"/>
        <Button icon="sap-icon://eraser" text="Clear" press="onClear" />
        <Button icon="sap-icon://display" text="Display" press="onDisplay"/>
        <Button icon="sap-icon://display" text="Read" press="onRead"/>
        <Button icon="sap-icon://create" text="Create" press="onCreate" />
        <Button icon="sap-icon://edit" text="Update" press="onUpdate" />
        <Button icon="sap-icon://delete" text="Delete" press="onDelete" />
    </OverflowToolbar>

    <f:SimpleForm id="inputForm" editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3"
                  labelSpanS="12" adjustLabelSpan="false" emptySpanXL="4" emptySpanL="4" emptySpanM="4" emptySpanS="0"
                  columnsXL="1" columnsL="1" columnsM="1" singleContainerFullSize="true" >

        <f:content>
            <Label text="Matnr" />
            <Input id="Matnr" width="6rem" maxLength="10"/>
            <Label text="Auart" />
            <Input id="Auart" width="6rem" maxLength="10"/>
            <Label text="Vkorg" />
            <Input id="Vkorg" width="6rem" maxLength="10"/>
            <Label text="Spart" />
            <Input id="Spart" width="6rem" maxLength="10"/>
        </f:content>


    </f:SimpleForm>

    </Page>
</mvc:View>